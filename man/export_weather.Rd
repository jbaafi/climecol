% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_weather.R
\name{export_weather}
\alias{export_weather}
\title{Export curated or simulated weather data}
\usage{
export_weather(df, path, meta = NULL, overwrite = TRUE)
}
\arguments{
\item{df}{A data frame or tibble containing weather data.}

\item{path}{File path to write the CSV output.}

\item{meta}{Named list of metadata fields (optional). Typical fields include:
\code{station}, \code{scenario}, \code{source}, and \code{version}.
If not provided, basic metadata (date and package version) are used.}

\item{overwrite}{Logical; if \code{TRUE}, overwrite existing file (default = TRUE).}
}
\value{
Invisibly returns the written file path.
}
\description{
Writes a weather dataset (observed, imputed, or scenario-based)
to a CSV file with standardized metadata for reproducibility.
}
\details{
This function behaves like \code{\link[utils:write.table]{utils::write.csv()}} but automatically
embeds key metadata (station, scenario, package version, and date)
as commented header lines at the top of the file.
}
\examples{
data(weather_nl)
tmpfile <- tempfile(fileext = ".csv")
export_weather(
  weather_nl,
  path = tmpfile,
  meta = list(station = "St. John's", scenario = "baseline")
)
readLines(tmpfile, n = 6)  # view header
}
