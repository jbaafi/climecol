% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_weather_csv.R
\name{read_weather_csv}
\alias{read_weather_csv}
\title{Read a raw weather CSV and standardize columns}
\usage{
read_weather_csv(
  path,
  mapping = default_weather_mapping(),
  station = NULL,
  tz = "UTC",
  na = c("NA", "", "M", "-9999", "-9999.9")
)
}
\arguments{
\item{path}{Path to the CSV file.}

\item{mapping}{Named character vector mapping \strong{raw CSV headers} to
standardized names. See \code{\link[=default_weather_mapping]{default_weather_mapping()}} for an example.
Names = raw column headers (any case/punctuation), values = one of the
standard names listed above.}

\item{station}{Optional character to override/set the \code{station} column.}

\item{tz}{Timezone used when parsing datetimes (if needed). Default "UTC".}

\item{na}{Character vector of strings to treat as missing.}
}
\value{
A tibble with the standard schema (columns present; some may be all NA).
}
\description{
Imports a raw weather CSV from \emph{any} source and returns a tibble with
standardized column names/units that other \code{climecol} functions can rely on.
You can pass a custom \code{mapping} to adapt to different header names;
by default, we include a mapping for Environment Canada daily files.

\strong{Standard schema returned (columns may be NA if not supplied):}
\itemize{
\item \code{date} (Date)
\item \code{station} (chr)
\item \code{climate_id} (chr)
\item \code{lon}, \code{lat} (numeric; degrees)
\item \code{tmax_c}, \code{tmin_c}, \code{tavg_c} (°C)
\item \code{rain_mm} (mm), \code{snow_cm} (cm), \code{precip_mm} (mm)
\item \code{snow_on_ground_cm} (cm)
\item \code{wind_spd_kmh} (km/h), \code{wind_dir_deg} (0–360)
}
}
\examples{
\dontrun{
df <- read_weather_csv("data-raw/ec_daily_2013_2023.csv")
head(df)
}
}
