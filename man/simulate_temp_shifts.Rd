% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_temp_shifts.R
\name{simulate_temp_shifts}
\alias{simulate_temp_shifts}
\title{Simulate temperature shift scenarios from a fitted seasonal curve}
\usage{
simulate_temp_shifts(
  fit,
  deltas = 0:5,
  model = "best",
  dates = NULL,
  as_long = TRUE
)
}
\arguments{
\item{fit}{A result list from \code{\link[=fit_seasonal_temp]{fit_seasonal_temp()}}.}

\item{deltas}{Numeric vector of °C increments to add (default 0:5).}

\item{model}{Which fitted model to use. Use \code{"best"} (default; lowest AIC)
or a specific model name present in \code{fit$metrics$model} (e.g., \code{"sin1"}).}

\item{dates}{Optional Date vector. If supplied, the fitted day-of-year curve
is mapped to these dates (using \code{lubridate::yday}); otherwise the native
\code{day_of_year} from \code{fit$daily_avg} (1–365) is used.}

\item{as_long}{Logical; if \code{TRUE} (default) return long format with columns
\code{key} (scenario label) and \code{temp_c}. If \code{FALSE}, return wide columns
\code{baseline}, \verb{Temp+1C}, \verb{Temp+2C}, ...}
}
\value{
A tibble with either \code{day_of_year} (no \code{dates}) or \code{date} (if
provided), plus simulated temperatures across scenarios.
}
\description{
Given the output of \code{\link[=fit_seasonal_temp]{fit_seasonal_temp()}}, generate temperature scenarios
by adding fixed deltas (e.g., +0 to +5 °C) to the fitted daily mean cycle.
}
\examples{
data(weather_nl)
fit <- fit_seasonal_temp(weather_nl, funcs = c("sin1","sin2"))
sims <- simulate_temp_shifts(fit, deltas = 0:5)      # long format over DOY
head(sims)

# Map to actual dates:
days <- seq(as.Date("2024-01-01"), as.Date("2024-12-31"), by = "day")
sims_dates <- simulate_temp_shifts(fit, deltas = c(0, 2, 5), dates = days, model = "best")
head(sims_dates)
}
