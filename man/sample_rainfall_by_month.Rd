% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_rainfall_by_month.R
\name{sample_rainfall_by_month}
\alias{sample_rainfall_by_month}
\title{Sample daily rainfall by month}
\usage{
sample_rainfall_by_month(
  dates,
  df,
  rain_col = NULL,
  drop_na = TRUE,
  na_as_zero = TRUE,
  replace = TRUE,
  origin = "2000-01-01",
  seed = NULL
)
}
\arguments{
\item{dates}{A vector of \code{Date}s to simulate for (one value returned per
date). Alternatively, a numeric/integer vector is treated as day offsets
from \code{origin}.}

\item{df}{A data frame of observed daily rainfall. Ideally contains
\code{date} and \code{rain_mm} (lowercase). If not, the function attempts to
standardize via \code{normalize_weather_names()}. If \code{date} is missing, an
integer \code{Month} or \code{month} column must exist.}

\item{rain_col}{Optional. Name of the rainfall column in \code{df}. By default,
tries \code{rain_mm} after normalization, then falls back to any column whose
name matches \code{"rain|precip"} (case-insensitive).}

\item{drop_na}{Logical; if \code{TRUE} (default) remove \code{NA} values from the
monthly sampling pools.}

\item{na_as_zero}{Logical; return 0 for sampled \code{NA}s (default \code{TRUE}).
Ignored when \code{drop_na = TRUE} since \code{NA}s are removed from pools.}

\item{replace}{Logical; sample with replacement. Default \code{TRUE}.}

\item{origin}{If \code{dates} is numeric, interpret as days since this origin
(default \code{"2000-01-01"}). Ignored if \code{dates} are \code{Date}s.}

\item{seed}{Optional integer for reproducible sampling. Default \code{NULL}.}
}
\value{
A numeric vector of length \code{length(dates)} with simulated daily rain (mm).
}
\description{
Generates a synthetic \strong{daily} rainfall series by resampling observed
daily values from the \strong{same calendar month} across years. This preserves
each month's empirical distribution (including dry-day frequency) without
imposing a parametric model.
}
\details{
The function is robust to varied input column names: it will first attempt
to standardize with \code{normalize_weather_names()}, then look for a rainfall
column (\code{rain_mm} by default) and derive the month from \code{date} if present.
}
\examples{
# Using the shipped example data
data(weather_nl)
set.seed(123)
dseq <- seq.Date(as.Date("2012-01-01"), as.Date("2012-01-10"), by = "day")
sim  <- sample_rainfall_by_month(dseq, weather_nl)
sim

# Numeric date offsets (days since origin)
sample_rainfall_by_month(0:9, weather_nl, origin = "2012-01-01")
}
